import {
    View,
    Text,
    TextInput,
    FlatList,
    ActivityIndicator,
    RefreshControl,
} from 'react-native';

const CreatePostScreen = (props) => {

    return (
        <View>

        </View>
    );
};

export default CreatePostScreen;